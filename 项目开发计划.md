# Atlas 边云协同智能体项目 — 后续开发计划

## 项目当前状态总览

项目已完成边云协同核心闭环，以下 8 个阶段全部实现：

| 阶段 | 内容 | 状态 |
|------|------|------|
| 阶段 1 | 项目初始化（Flask + LangChain Agent + Vue 3） | ✅ |
| 阶段 2 | Atlas YOLO 入口确认（OM 模型推理 + 16.54 FPS 实测） | ✅ |
| 阶段 3 | 边端脚本封装（atlas_yolo_detect_and_upload.py + atlas_upload_client.py） | ✅ |
| 阶段 4 | 边云通信接口（16 个 RESTful API + 心跳 + 数据持久化） | ✅ |
| 阶段 5 | 云端智能体改造（DeepSeek 文本 + SenseNova 多模态 + RAG） | ✅ |
| 阶段 6 | 管理平台改造（Vue 3 双模式 + 设备监控 + 任务流 + SSH 控制） | ✅ |
| 阶段 7 | Docker 容器化（Dockerfile + docker-compose.yml） | ✅ |
| 阶段 8 | 前端重构（CSS 拆分为 4 文件 + JS 拆分为 5 模块） | ✅ |

与课程设计要求对比后，**尚需完成的 3 项功能缺口**见下方。

---

## 剩余任务 1：Agent 联网搜索工具

### 背景

课程要求 3.2："智能体可调用搜索工具获取实时信息"。当前 Agent 的工具箱覆盖了天气/交通/航班/酒店/POI/火车票等 18+ 领域专用 API，但**没有通用互联网搜索工具**——无法回答诸如"大连有什么新开的商场""星海广场最近有没有活动"这类需要实时网页信息的问题。

### 实现方案

新增一个 DuckDuckGo Instant Answer / Text Search 工具，无需 API Key，免费可用。

#### 新增文件

`src/travel_agent/tool_web_search.py`（新文件，约 60 行）

核心函数：
```python
from langchain_core.tools import tool
from duckduckgo_search import DDGS

@tool
def web_search(query: str, max_results: int = 5) -> str:
    """使用 DuckDuckGo 搜索互联网，获取旅游相关的实时信息。
    
    当用户询问以下内容时应使用此工具：
    - 目的地的最新活动、新开景点、节庆信息
    - 景点开放时间变更、门票价格更新
    - 当地政策（交通管制、景区限流等）
    - 其他工具无法覆盖的实时信息
    
    Args:
        query: 搜索关键词，建议包含城市名+具体问题
        max_results: 返回结果数量，默认 5
    """
    try:
        with DDGS() as ddgs:
            results = list(ddgs.text(query, max_results=max(max_results, 1)))
        if not results:
            return f"搜索 '{query}' 未找到相关结果。"
        lines = [f"数据源：DuckDuckGo 搜索\n查询：{query}"]
        for i, r in enumerate(results, 1):
            lines.append(
                f"{i}. {r.get('title', '无标题')}\n"
                f"   {r.get('body', '')}\n"
                f"   链接：{r.get('href', '')}"
            )
        return "\n".join(lines)
    except Exception as exc:
        return f"联网搜索异常：{exc}"
```

#### 依赖安装

```bash
pip install duckduckgo-search
```

并将 `duckduckgo-search` 追加到 `requirements.txt` 末尾。

#### 注册到 Agent

修改 `src/travel_agent/tools.py`，两处改动：

1. 在文件顶部 import：
```python
from travel_agent.tool_web_search import web_search
```

2. 在 `ALL_TOOLS` 列表（约 L90-L110）中加入 `web_search`：
```python
ALL_TOOLS = [
    # ... 现有工具 ...
    web_search,  # 新增：DuckDuckGo 联网搜索
]
```
同时在 `get_weather_tools()` 返回列表中也添加（或放在通用工具列表中）。

#### 验证方式

重启 Flask，发送对话消息："帮我搜索一下大连最近有什么新开的景点"，观察 trace 面板中出现 `web_search` 工具调用的搜索结果。

---

## 剩余任务 2：实时视频流 + YOLO 检测框预览

### 背景

课程要求 3.4："展示边端摄像头画面及 YOLO 检测框"。当前管理平台可以在任务详情弹窗查看**静态标注图片/视频帧**，但缺少**实时视频流预览**面板——这是答辩演示时视觉冲击力最强的功能。

### 方案选择

Atlas 开发板通过 `atlas_yolo_detect_and_upload.py --camera 0` 持续采集摄像头帧 → 每 N 帧进行一次 YOLO 推理 → 将**最近一帧的标注图**上传到云端 `/api/edge/heartbeat` 或专用的帧上传端点 → 前端通过定时轮询获取最新帧 URL → `<img>` 标签展示。

简化方案（无需 WebSocket/MJPEG 流，利用现有心跳基础设施）：

#### 后端新增

在 `server/edge_routes.py` 中新增一个轻量端点：

```python
@edge_bp.route("/api/edge/latest-frame", methods=["GET"])
def get_latest_frame():
    """返回边端最新摄像头帧的标注图 URL。
    
    边端通过心跳上报最新帧路径（base64 或文件路径），
    服务端缓存最近一帧的 URL。
    """
    # 从缓存中读取最新帧信息
    latest = _device_cache.get("latest_frame")
    if not latest:
        return jsonify({"ok": False, "message": "暂无实时帧数据"}), 404
    return jsonify({"ok": True, "frame_url": latest.get("url", ""),
                    "timestamp": latest.get("timestamp", ""),
                    "fps": latest.get("fps", None),
                    "detections_count": latest.get("detections_count", 0)})
```

在心跳处理 (`POST /api/edge/heartbeat`) 中增加一个可选字段 `latest_frame_b64`：

- 如果心跳携带 base64 编码的标注帧图片 → 解码保存到 `data/edge_artifacts/live_frame.jpg`
- 缓存帧 URL 和时间戳
- 如果心跳不带帧数据（仅系统指标），跳过帧更新

#### 前端新增

在 `EdgeMonitor.js` Dashboard 顶部或任务列表上方新增**实时预览区域**：

```html
<!-- 实时视频预览卡片（放在 EdgeMonitor template 顶部） -->
<div v-if="yoloInferenceStatus === 'running_board'" class="live-preview-card">
  <div class="live-preview-header">
    <span class="live-dot"></span>
    <span>实时 YOLO 推理预览</span>
    <span class="live-fps" v-if="latestFrame.fps">{{ latestFrame.fps }} FPS</span>
    <span class="live-detections">{{ latestFrame.detections_count || 0 }} 目标</span>
  </div>
  <div class="live-preview-viewport">
    <img v-if="latestFrame.url" :src="latestFrame.url"
         class="live-frame-image"
         alt="实时 YOLO 推理帧" />
    <div v-else class="live-frame-placeholder">
      等待边端摄像头帧...
    </div>
  </div>
</div>
```

用 `setInterval(loadLatestFrame, 1000)` 每秒轮询 `/api/edge/latest-frame`。

#### CSS

在 `static/styles/edge.css` 新增约 40 行样式（`.live-preview-card` 等），匹配现有 Dashboard 卡片风格。

#### 边端改动

在 `edge/atlas_yolo_detect_and_upload.py` 的 `--camera` 模式下，每帧推理后将标注图编码为 base64，在心跳请求中附带 `latest_frame_b64` 字段。如果标注图 >200KB，先缩放到 640px 宽再编码（避免心跳包过大）。

#### 验证方式

1. Atlas 端启动：`python3 atlas_yolo_detect_and_upload.py --camera 0 --server http://192.168.0.101:5000 --upload`
2. 浏览器打开 `http://127.0.0.1:5000` → 边云协同模式
3. Dashboard 顶部出现实时预览卡片，显示最近的 YOLO 推理帧，每秒更新

---

## 剩余任务 3：边端断网重连机制

### 背景

课程要求 3.1："网络波动时边缘端应能独立运行基础检测功能"。当前项目**已有**：上传失败后的指数退避重试（2/4/8 秒）和 `pending_events/` 离线队列（彻底失败时落盘）。但**缺少**：

1. 心跳断连时的**自主检测恢复**——目前 `--heartbeat --watch` 模式下心跳失败后会继续尝试，但不会在恢复后**自动重试 pending 队列中的历史事件**
2. 检测推理模式下，上传失败存入 `pending_events/` 后，**没有后台守护线程**在连接恢复时自动清理重传队列

### 实现方案

#### 边端改动

修改 `edge/atlas_upload_client.py`，在心跳 watch 循环中增加以下逻辑：

```python
def watch_heartbeat(server_url: str, interval: int, timeout: int):
    """持续心跳 + 自动检测恢复 + 清理 pending 队列"""
    last_success = None
    was_offline = False
    
    while True:
        try:
            metrics = collect_system_metrics()
            payload = {"device_id": DEVICE_ID, "metrics": metrics, ...}
            if collect_npu_metrics:
                payload["npu"] = collect_npu_metrics()
            resp = urllib.request.urlopen(
                urllib.request.Request(url, data=json.dumps(payload).encode(), ...)
            )
            # 心跳成功
            last_success = time.time()
            
            if was_offline:
                # ★ 刚从断网恢复 → 自动重试 pending 队列
                was_offline = False
                print("[reconnect] 网络已恢复，自动重试离线队列...")
                pending_count = len(load_pending_events())
                if pending_count > 0:
                    retry_pending_events(server_url, timeout)
            
        except Exception as exc:
            if not was_offline:
                # ★ 首次检测到断网
                was_offline = True
                print(f"[offline] 无法连接云端 {server_url}，进入离线模式。错误: {exc}")
        
        time.sleep(interval)
```

这个逻辑的核心是：
- 维护 `was_offline` 状态标志
- 心跳成功 + 之前标记为离线 → 判定为"恢复" → 调用 `retry_pending_events()` 清空离线队列
- 心跳失败 + 之前标记为在线 → 判定为"断网" → 记录日志，边端继续本地检测

#### 后端改动（可选增强）

在 Cloud 端心跳处理中增加 `pending_count` 字段的存储，前端 Dashboard 显示"待同步事件数"——当数字 >0 时高亮提示用户边端有离线数据等待回传。

`server/edge_routes.py` 心跳处理中已有的逻辑无需修改，只需确认前端 `SidebarComponent.js` 的 device 数据中能读取到 `pending_events` 计数并展示。

#### 验证方式

1. Atlas 启动持续心跳 + 检测模式
2. 拔掉 Atlas 网线 / 关闭笔记本 Flask 服务 → 边端日志显示 `[offline] 进入离线模式`
3. Atlas 继续本地推理（`--camera` 模式仍然工作，检测结果存入 `pending_events/`）
4. 恢复网络 → 边端日志显示 `[reconnect] 网络已恢复，自动重试离线队列`
5. 笔记本 Dashboard 出现之前暂存的检测任务

---

## 实施优先级与建议顺序

```
1. Agent 联网搜索工具（最简单，1 个新文件 + 1 行 import + 1 行 register）
   ↓ 耗时约 30 分钟
2. 断网重连（修改 1 个边端文件，纯逻辑增强）
   ↓ 耗时约 1 小时
3. 实时视频流预览（涉及前后端 + 边端三方，最复杂）
   耗时约 2-3 小时
```

建议按顺序逐个完成，每个完成后提交一次。

---

## 不再实施的项目

以下内容已确认**不需要做**：

- ~~MQTT 通信协议~~（采纳 HTTP/RESTful，课程要求中为可选建议"建议 MQTT 或 HTTP"）
- ~~系统架构图/海报/API 文档完善/Demo 视频录制~~（由用户自行完成交付成果文档）
- ~~Docker 完善~~（Dockerfile 已有但无法验证，非当前重点）
- ~~多设备管理~~（只有一台 Atlas 开发板）
- ~~FPS 实时面板~~（低优先级，可在视频流预览中间接展示）
