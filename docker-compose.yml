services:
  atlas-cloud-agent:
    build: .
    container_name: atlas-edge-cloud-agent
    env_file:
      - .env
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
